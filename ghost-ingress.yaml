apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ghosttest-ingress
spec:
  rules:
  - http:
      paths:
      - path: /*
        pathType: ImplementationSpecific
        backend:
          service:
            name: ghost
            port:
              number: 80